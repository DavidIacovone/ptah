/**
 * ptah list — List all configured Ptah projects
 *
 * Stub: implemented in Plan 02.
 */

export async function runList(_args: string[]): Promise<void> {
  console.log('ptah list — not yet implemented (Plan 02)');
  process.exit(0);
}
