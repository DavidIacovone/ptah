/**
 * ptah init — Initialize a new Ptah project
 *
 * Stub: implemented in Plan 02.
 */

export async function runInit(_args: string[]): Promise<void> {
  console.log('ptah init — not yet implemented (Plan 02)');
  process.exit(0);
}
